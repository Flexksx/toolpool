set working-directory := "../.."

[private]
default:
    just --list --justfile {{source_file()}}

# Build every toolpool library module
toolpool:
    moon run '#toolpool-lib:build'

# Build the sample-rest-api-client lib
sample-rest-api-client:
    moon run sample-rest-api-client:build

# Build the toolpool-demo app
toolpool-demo:
    moon run toolpool-demo:build

# Build every unit
all:
    moon run ':build'
