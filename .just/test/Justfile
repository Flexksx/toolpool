set working-directory := "../.."

[private]
default:
    just --list --justfile {{source_file()}}

# Run the toolpool test suite
toolpool:
    moon run toolpool:test

# Run the sample-rest-api-client test suite
sample-rest-api-client:
    ./gradlew :libs:sample-rest-api-client:test

# Run the toolpool-demo test suite
toolpool-demo:
    moon run toolpool-demo:test

# Run every test suite
all: toolpool sample-rest-api-client toolpool-demo
