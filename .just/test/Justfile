set working-directory := "../.."

[private]
default:
    just --list --justfile {{source_file()}}

# Run the test suite of every toolpool library module
toolpool:
    moon run '#toolpool-lib:test'

# Run the sample-rest-api-client test suite
sample-rest-api-client:
    moon run sample-rest-api-client:test

# Run the toolpool-demo test suite
toolpool-demo:
    moon run toolpool-demo:test

# Run the cross-module architecture rules
architecture:
    moon run architecture-test:test

# Run every test suite
all:
    moon run ':test'
