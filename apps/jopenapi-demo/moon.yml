$schema: "https://moonrepo.dev/schemas/project.json"
language: "unknown"
layer: "application"
dependsOn:
  - jopenapimcp
tasks:
  build:
    command: "./gradlew"
    args: [":apps:jopenapi-demo:build"]
    deps: ["^:build"]
    options:
      runFromWorkspaceRoot: true
  test:
    command: "./gradlew"
    args: [":apps:jopenapi-demo:test"]
    deps: ["^:build"]
    options:
      runFromWorkspaceRoot: true
  run:
    command: "./gradlew"
    args: [":apps:jopenapi-demo:bootRun"]
    deps: ["^:build"]
    options:
      runFromWorkspaceRoot: true
