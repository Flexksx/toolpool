spring:
  application:
    name: toolpool-demo
  ai:
    mcp:
      server:
        enabled: true
        version: 1.0.0

toolpool:
  # metatools = tool_search/read_tool/tool_call gateway; direct = one MCP tool per operation
  mode: metatools
  spec-location: ./openapi/sample-rest-api-client.openapi.json
  base-url: http://localhost:18080
  refresh-interval: 5m
